import '@styles.css';

function FontSpecimen({
  name,
  specimen,
  className,
}: {
  name: string;
  specimen: string;
  className: string;
}) {
  return (
    <div className="flex items-baseline gap-4">
      <p className="w-16 shrink-0 font-mono text-[10px] text-neutral-400">
        {name}
      </p>
      <p className={`text-2xl text-neutral-800 ${className}`}>{specimen}</p>
    </div>
  );
}

function ScaleSample({ label, className }: { label: string; className: string }) {
  return (
    <div className="flex items-baseline gap-4">
      <p className="w-16 shrink-0 text-right font-mono text-[10px] text-neutral-400">
        {label}
      </p>
      <p className={`truncate leading-tight text-neutral-900 ${className}`}>
        The quick brown fox
      </p>
    </div>
  );
}

export default function Typography() {
  return (
    <div className="h-full bg-white p-14">
      <h1 className="mb-8 border-b border-neutral-200 pb-3 text-3xl font-bold text-neutral-900">
        Typography
      </h1>

      {/* Font families */}
      <div className="flex flex-col gap-4">
        <FontSpecimen
          name="Sans"
          specimen="Clean, modern, and endlessly versatile"
          className="font-sans"
        />
        <FontSpecimen
          name="Display"
          specimen="Elegant presence in every headline"
          className="font-display"
        />
        <FontSpecimen
          name="Serif"
          specimen="Timeless sophistication meets digital craft"
          className="font-serif"
        />
        <FontSpecimen
          name="Mono"
          specimen="console.log('precision matters')"
          className="font-mono"
        />
      </div>

      {/* Divider */}
      <div className="my-8 border-t border-neutral-100" />

      {/* Type scale */}
      <p className="mb-5 text-xs font-semibold uppercase tracking-widest text-neutral-400">
        Scale
      </p>
      <div className="flex flex-col gap-2.5">
        <ScaleSample label="7xl" className="text-7xl" />
        <ScaleSample label="6xl" className="text-6xl" />
        <ScaleSample label="5xl" className="text-5xl" />
        <ScaleSample label="4xl" className="text-4xl" />
        <ScaleSample label="3xl" className="text-3xl" />
        <ScaleSample label="2xl" className="text-2xl" />
        <ScaleSample label="xl" className="text-xl" />
        <ScaleSample label="lg" className="text-lg" />
        <ScaleSample label="base" className="text-base" />
        <ScaleSample label="sm" className="text-sm" />
        <ScaleSample label="xs" className="text-xs" />
      </div>
    </div>
  );
}
