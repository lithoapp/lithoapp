import { TerminalWindow, Lightning, Database } from '@phosphor-icons/react';

export default function BrandInAction() {
  return (
    <div className="h-full bg-primary-950 p-14">
      <div className="flex items-center gap-3 mb-8">
        <span className="font-mono text-sm text-accent-500">05</span>
        <h1 className="font-display text-4xl font-bold text-neutral-50 tracking-tight">Brand in Action</h1>
      </div>

      <div className="space-y-8">
        <div className="rounded-2xl bg-neutral-900 border border-neutral-800 overflow-hidden">
          <div className="flex items-center gap-2 px-5 py-3 border-b border-neutral-800 bg-neutral-950">
            <div className="w-3 h-3 rounded-full bg-error-500/70" />
            <div className="w-3 h-3 rounded-full bg-warning-500/70" />
            <div className="w-3 h-3 rounded-full bg-success-500/70" />
            <span className="ml-3 font-mono text-sm text-neutral-500">platform overview</span>
          </div>

          <div className="p-8 grid grid-cols-3 gap-5">
            {[
              { title: 'API Calls', value: '1.2M', detail: '+12% this week', tone: 'text-accent-400', glow: 'shadow-accent-500/20' },
              { title: 'Latency', value: '42ms', detail: 'p95 stable', tone: 'text-secondary-400', glow: 'shadow-secondary-500/20' },
              { title: 'Uptime', value: '99.9%', detail: 'last 30 days', tone: 'text-success-400', glow: '' },
            ].map(({ title, value, detail, tone, glow }) => (
              <div key={title} className={`rounded-xl bg-neutral-800 border border-neutral-700 p-5 shadow-md ${glow}`}>
                <p className="font-mono text-xs text-neutral-500 uppercase tracking-widest mb-2">{title}</p>
                <p className={`font-display text-3xl font-semibold ${tone}`}>{value}</p>
                <p className="font-mono text-sm text-neutral-600 mt-1">{detail}</p>
              </div>
            ))}
          </div>
        </div>

        <div className="rounded-2xl bg-neutral-900 border border-neutral-800 p-8">
          <p className="font-mono text-sm text-neutral-400 uppercase tracking-widest mb-5">Endpoint Activity</p>
          <div className="rounded-xl bg-neutral-950 border border-neutral-800 divide-y divide-neutral-800">
            {[
              { method: 'GET', path: '/api/v2/users', status: '200 OK', time: '18ms' },
              { method: 'POST', path: '/api/v2/events', status: '201 Created', time: '34ms' },
              { method: 'GET', path: '/api/v2/metrics', status: '200 OK', time: '12ms' },
              { method: 'PATCH', path: '/api/v2/settings', status: '204 No Content', time: '26ms' },
            ].map(({ method, path, status, time }) => (
              <div key={path} className="flex items-center gap-4 px-5 py-4">
                <span className={`font-mono text-sm w-14 ${method === 'GET' ? 'text-accent-400' : method === 'POST' ? 'text-secondary-400' : 'text-warning-400'}`}>{method}</span>
                <span className="font-mono text-base text-neutral-200 flex-1">{path}</span>
                <span className="font-mono text-sm text-success-500">{status}</span>
                <span className="font-mono text-sm text-neutral-600">{time}</span>
              </div>
            ))}
          </div>
        </div>

        <div className="rounded-2xl bg-neutral-900 border border-secondary-500/25 p-8 shadow-lg shadow-secondary-500/10">
          <p className="font-mono text-sm text-secondary-400 uppercase tracking-widest mb-5">Manifesto</p>
          <div className="grid grid-cols-3 gap-6">
            <div className="flex gap-3">
              <TerminalWindow size={20} weight="duotone" className="text-accent-400 mt-0.5" />
              <p className="font-sans text-sm text-neutral-300">Code patterns are part of the brand voice, not decoration.</p>
            </div>
            <div className="flex gap-3">
              <Lightning size={20} weight="duotone" className="text-secondary-400 mt-0.5" />
              <p className="font-sans text-sm text-neutral-300">Glow is intentional: active, selected, or high-priority moments only.</p>
            </div>
            <div className="flex gap-3">
              <Database size={20} weight="duotone" className="text-accent-400 mt-0.5" />
              <p className="font-sans text-sm text-neutral-300">Dark surfaces and strong hierarchy make technical content feel premium.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
