export default function Cover() {
  return (
    <div className="h-full bg-primary-950 p-16 flex flex-col justify-between relative overflow-hidden">
      {/* Subtle gradient overlay */}
      <div className="absolute inset-0 bg-gradient-to-br from-secondary-950/40 via-transparent to-accent-950/30" />

      {/* Top section */}
      <div className="relative z-10">
        <div className="inline-flex items-center gap-2 rounded-full border border-accent-500/30 bg-accent-500/10 px-3 py-1.5 mb-6">
          <span className="h-2 w-2 rounded-full bg-accent-400 animate-pulse" />
          <span className="font-mono text-xs text-accent-400">v1.0.0</span>
        </div>

        <h1 className="font-display text-6xl font-bold text-neutral-50 tracking-tight mb-4">
          {{{workspaceName}}}
        </h1>
        <h2 className="font-display text-4xl font-light text-secondary-400 tracking-tight">
          Design System
        </h2>
      </div>

      {/* Terminal-style decoration */}
      <div className="relative z-10 font-mono text-sm text-neutral-600 space-y-1">
        <p><span className="text-secondary-500">$</span> init design-system</p>
        <p><span className="text-accent-500">→</span> loading foundations...</p>
        <p><span className="text-accent-500">→</span> colors, typography, spacing</p>
        <p><span className="text-success-500">✓</span> system ready</p>
      </div>

      {/* Bottom info bar */}
      <div className="relative z-10 flex items-end justify-between pt-8 border-t border-neutral-800">
        <div>
          <p className="font-mono text-xs text-neutral-500 uppercase tracking-widest mb-1">Brand Handbook</p>
          <p className="font-mono text-xs text-neutral-600">Built for developers who appreciate great DX</p>
        </div>
        <div className="text-right">
          <p className="font-mono text-xs text-neutral-500 uppercase tracking-widest mb-1">Last Updated</p>
          <p className="font-mono text-xs text-neutral-600">{{{currentYear}}}</p>
        </div>
      </div>
    </div>
  );
}
