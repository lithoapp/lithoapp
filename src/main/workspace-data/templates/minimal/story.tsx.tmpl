import '@styles.css';

export default function Story() {
  return (
    <div className="flex h-full flex-col bg-white px-16 py-12">
      {/* Header */}
      <div className="flex flex-col gap-2">
        <p className="text-xs font-medium uppercase tracking-[0.2em] text-neutral-400">
          03 — Story
        </p>
        <h2 className="font-display text-4xl font-semibold tracking-tight text-neutral-900">
          Brand Story
        </h2>
        <p className="mt-1 max-w-lg text-base text-neutral-500">
          This page is yours to fill. Use the AI assistant to define your brand voice, values, and personality.
        </p>
      </div>

      {/* Placeholder content */}
      <div className="mt-16 flex flex-1 flex-col items-center justify-center">
        <div className="flex max-w-md flex-col items-center gap-6 text-center">
          <div className="h-px w-16 bg-neutral-200" />
          <p className="text-lg leading-relaxed text-neutral-400">
            Start a conversation with the design system agent to craft your brand narrative here.
          </p>
          <div className="h-px w-16 bg-neutral-200" />
        </div>
      </div>
    </div>
  );
}
