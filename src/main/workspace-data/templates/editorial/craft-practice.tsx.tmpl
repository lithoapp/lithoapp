import '@styles.css';
import { Quotes, ArrowRight, Sparkle } from '@phosphor-icons/react';

export default function CraftAndPractice() {
  return (
    <div className="h-full flex flex-col bg-neutral-50 px-16 py-14">

      {/* Header */}
      <p className="text-xs font-semibold uppercase tracking-[0.25em] text-accent-600">Craft & Practice</p>
      <h2 className="mt-2 font-display text-4xl font-semibold text-neutral-900">
        The brand in motion.
      </h2>
      <p className="mt-3 font-sans text-sm text-neutral-400 max-w-lg">
        How colour, type, and space perform together — across real layouts, at every scale.
      </p>

      {/* Shape & elevation */}
      <div className="mt-10">
        <p className="text-[10px] font-semibold uppercase tracking-widest text-neutral-400 mb-5">Shape & Elevation</p>
        <div className="flex gap-5">
          <div className="flex-1 rounded-lg bg-white border border-neutral-100 shadow-sm p-6">
            <div className="h-2 w-8 rounded-full bg-primary-300 mb-4" />
            <p className="font-display text-lg font-medium text-neutral-800">Subtle</p>
            <p className="mt-2 font-sans text-xs leading-relaxed text-neutral-400">Soft corners, quiet shadow. The default for content blocks and supporting layouts.</p>
          </div>
          <div className="flex-1 rounded-2xl bg-white shadow-md p-6">
            <div className="h-2 w-8 rounded-full bg-accent-400 mb-4" />
            <p className="font-display text-lg font-medium text-neutral-800">Elevated</p>
            <p className="mt-2 font-sans text-xs leading-relaxed text-neutral-400">More curve, more lift. For featured content that needs to rise above the page.</p>
          </div>
          <div className="flex-1 rounded-3xl bg-primary-700 shadow-xl p-6">
            <div className="h-2 w-8 rounded-full bg-accent-300 mb-4" />
            <p className="font-display text-lg font-medium text-white">Hero</p>
            <p className="mt-2 font-sans text-xs leading-relaxed text-primary-300">Full rounding, rich background. The centrepiece of any layout.</p>
          </div>
        </div>
      </div>

      {/* Editorial layout sample */}
      <div className="mt-8 flex gap-5">

        {/* Pull quote block */}
        <div className="flex-1 rounded-2xl bg-neutral-900 p-8">
          <Quotes size={28} weight="duotone" color="var(--color-accent-400)" />
          <p className="mt-6 font-display text-2xl font-normal italic leading-snug text-white">
            Beauty is in the details — and in the space between them.
          </p>
          <p className="mt-5 font-sans text-xs font-semibold uppercase tracking-widest text-neutral-500">{{{workspaceName}}}</p>
        </div>

        {/* Spacing rhythm */}
        <div className="flex-1 rounded-2xl bg-white border border-neutral-100 shadow-sm p-8">
          <p className="text-[10px] font-semibold uppercase tracking-widest text-neutral-400 mb-5">Spacing Rhythm</p>
          <div className="flex flex-col gap-0">
            {[
              { label: '4px', w: 'w-2', shade: 'bg-primary-100' },
              { label: '8px', w: 'w-4', shade: 'bg-primary-200' },
              { label: '16px', w: 'w-8', shade: 'bg-primary-300' },
              { label: '24px', w: 'w-12', shade: 'bg-primary-400' },
              { label: '32px', w: 'w-16', shade: 'bg-primary-500' },
              { label: '48px', w: 'w-24', shade: 'bg-accent-400' },
            ].map(({ label, w, shade }) => (
              <div key={label} className="flex items-center gap-4 py-1.5 border-b border-neutral-100 last:border-0">
                <div className={`h-2 rounded-full ${w} ${shade}`} />
                <p className="font-mono text-[10px] text-neutral-400">{label}</p>
              </div>
            ))}
          </div>
          <div className="flex items-center gap-2 mt-3">
            <ArrowRight size={12} color="var(--color-accent-500)" />
            <p className="font-sans text-[10px] text-neutral-400">Proportional multiples — consistent, rhythmic, intentional.</p>
          </div>
        </div>

      </div>

      {/* Closing quote */}
      <div className="mt-8 rounded-2xl bg-primary-800 p-8 flex items-center justify-between gap-12">
        <div className="flex items-start gap-5">
          <Sparkle size={28} weight="duotone" color="white" className="mt-1 shrink-0" />
          <p className="font-display text-3xl font-normal italic leading-snug text-white">
            "Every brand has a story worth telling beautifully."
          </p>
        </div>
        <div className="shrink-0 text-right">
          <p className="font-sans text-xs font-semibold uppercase tracking-widest text-primary-300">{{{workspaceName}}}</p>
          <div className="flex gap-2 mt-3 justify-end">
            <div className="h-2 w-2 rounded-full bg-primary-400" />
            <div className="h-2 w-2 rounded-full bg-accent-400" />
            <div className="h-2 w-2 rounded-full bg-secondary-400" />
          </div>
        </div>
      </div>
    </div>
  );
}
