Commit all changes with an appropriate commit message. Update CLAUDE.md if any architectural patterns, conventions, or project structure changed. Finally, call the `mcp__cortex__concludeSession` tool to conclude the session.

